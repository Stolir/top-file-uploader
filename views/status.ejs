<section class="status-container">
  <h1 style="text-align: center">
    <%= status.code ? `${status.code} | ${status.msg}` : status.msg %>
  </h1>
  <a href="<%= redirect.path %>"><%= redirect.msg %></a>
</section>

